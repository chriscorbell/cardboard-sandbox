# Agent instructions

This is a plain static site. Edit `index.html` and `styles.css` directly; there is no build,
package manager, or test suite. Before opening a pull request, run `./check.sh`, which validates
that the HTML still parses and that every stylesheet it references exists.

Keep changes small and self-contained. One card, one pull request.
