# Cardboard sandbox

A throwaway static site used to exercise Cardboard end to end: a Session clones this repository,
changes it on a branch, opens a pull request, and Cardboard merges it on Approval.

Open `index.html` in a browser. There is no build step.
